openapi: 3.0.1
info:
  title: PlaceOrderBackend
  description: API Definition of PlaceOrderBackend
  version: 1.0.0
servers:
- url: http://localhost:8290/rocco-backend
paths:
  /placeorder:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
